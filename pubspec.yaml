name: sniperking
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.12.0
  firebase_database: ^11.3.0
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: "^0.13.1" # เพิ่มบรรทัดนี้เข้ามา

flutter:
  uses-material-design: true

  assets:
    - assets/images/Ironman (2).jpg
    - assets/images/IMG_20260922_194127.jpg
    - assets/images/IMG_20260922_200506.jpg

# เพิ่มส่วนตั้งค่าไอคอนนี้ไว้ล่างสุดของไฟล์
flutter_launcher_icons:
  image_path: "assets/images/Ironman (2).jpg" # เลือกรูปที่ต้องการใช้เป็นไอคอน
  android: true
  ios: true
